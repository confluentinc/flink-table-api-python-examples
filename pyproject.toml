[project]
name = "flink_table_api_python"
version = "0.1.0"
description = "Examples for using Python Table API with Confluent Cloud"
authors = [
    {name = "Confluent"}
]
license = {text = "Apache-2.0"}
readme = "README.md"
requires-python = ">=3.9,<3.12"
dependencies = [
    "confluent-pyflink>=2.3.3",
]

[dependency-groups]
lint = ["ruff"]

[tool.uv]
# Pin the build backend below that so beam compiles when it has no platform wheel (e.g. local macOS).
build-constraint-dependencies = ["setuptools<81"]

[tool.ruff]
line-length = 99
indent-width = 4
target-version = "py39"
extend-exclude = ["setup_pyshell.py"]

[tool.ruff.lint]
select = ["E", "F"]
ignore = []

fixable = ["ALL"]
unfixable = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"
